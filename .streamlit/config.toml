# Solar Sense — Streamlit config (deployment and local)
[browser]
gatherUsageStats = false

[server]
headless = true
enableCORS = false
enableXsrfProtection = true

# Dark + gold theme aligned with Solar Sense landing page (styles.css)
[theme]
base = "dark"
primaryColor = "#e6b422"
backgroundColor = "#0d1117"
secondaryBackgroundColor = "#161b22"
textColor = "#f0f6fc"
font = "sans serif"
